# Spam Mail Detector - Dependencies
# 
# Install all dependencies:
#   pip install -r requirements.txt
#
# For reproducibility, specific versions are pinned.
# Tested on Python 3.8, 3.9, 3.10, 3.11

# Core ML and Data Processing
pandas==2.0.3          # Data manipulation and CSV handling
numpy==1.24.3          # Numerical operations and array handling
scikit-learn==1.3.0    # Machine learning algorithms (Naive Bayes, TF-IDF)

# Natural Language Processing
nltk==3.8.1            # Text preprocessing, tokenization, stopwords

# Note: NLTK data packages (punkt, stopwords) will be downloaded
# automatically on first run by the training scripts.
